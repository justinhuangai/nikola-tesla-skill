# 交流电系统、感应与多相 / AC Systems, Induction, and Polyphase

The engineering logic of Tesla's AC work, rotating fields, induction motors, and polyphase systems.

## Why This File Exists

- This file supports the Nikola Tesla person skill by making the research layer thick enough to guide real reasoning rather than surface style.
- It treats 特斯拉 as a structured cognitive system, not as a quotation source or prestige label.
- The focus of this file is The engineering logic of Tesla's AC work, rotating fields, induction motors, and polyphase systems..
- The notes below are organized to keep historical grounding, operational transfer, and misuse boundaries visible at the same time.

## Source Orientation

- Read the primary texts or documentary anchors first, then map them to later interpretation.
- Keep biography, corpus boundary, and later myth separate from direct conceptual claims.
- When transferring this material into a modern skill, preserve the structure and drop the historical costume.

## Research Notes

### rotating magnetic field — Definition

- What this names: within `The engineering logic of Tesla's AC work, rotating fields, induction motors, and polyphase systems`, `rotating magnetic field` points to a concrete node rather than a prestige label.
- Object boundary: treat `rotating magnetic field` as a research handle with limits, not as a ready-made conclusion.
- Why it stays in the file: `rotating magnetic field` changes how the figure is periodized, sourced, or operationalized.
- How to read it: start from the historical referent of `rotating magnetic field` before pulling it into a modern analogy.
- Skill value: `rotating magnetic field` gives the repo a sharper diagnostic vocabulary for judgment and transfer.
- Reader warning: when `rotating magnetic field` is reduced to a slogan, the file loses the distinction it was built to protect.
- Practical use: define `rotating magnetic field` clearly enough that a later answer can say what `rotating magnetic field` includes and excludes.

### rotating magnetic field — Historical Context

- Historical location: ask when `rotating magnetic field` first becomes visible in the surviving record and through which institution, text, or episode.
- Surrounding pressure: `rotating magnetic field` usually sits inside a wider dispute about authority, method, legitimacy, or transmission.
- Context clue: the point is not only what `rotating magnetic field` says, but why `rotating magnetic field` mattered in its own setting.
- Corpus implication: historical placement helps distinguish stable doctrine around `rotating magnetic field` from later packaging or selective reuse.
- Reading move: map `rotating magnetic field` against the timeline before treating `rotating magnetic field` as timeless method.
- Transfer discipline: preserve the contextual problem that produced `rotating magnetic field` before extracting any lesson from `rotating magnetic field`.
- Failure risk: without context, `rotating magnetic field` can look cleaner, more universal, and more innocent than it actually was.

### rotating magnetic field — Internal Tension

- Main tension: `rotating magnetic field` usually bundles a capability gain with a distortion cost instead of offering one clean virtue.
- Diagnostic split: ask what `rotating magnetic field` makes possible and what `rotating magnetic field` quietly suppresses at the same time.
- Interpretive value: the tension in `rotating magnetic field` matters because this repo is meant to preserve pressure, not just preference.
- Operational lesson: strong skills keep `rotating magnetic field` double-sided rather than converting `rotating magnetic field` into moral reassurance.
- Research warning: if every sentence about `rotating magnetic field` sounds admiring or dismissive, `rotating magnetic field` has already been flattened.
- Transfer rule: modern answers should name both the leverage and the blind spot living inside `rotating magnetic field`.
- Better question: what does `rotating magnetic field` solve, and what new silence does `rotating magnetic field` create?

### rotating magnetic field — Key Distinction

- Core distinction: separate `rotating magnetic field` from adjacent ideas that look similar but solve a different problem.
- Why this matters: a person skill only becomes precise when near-miss concepts stop leaking into `rotating magnetic field`.
- Typical confusion: readers often mistake `rotating magnetic field` for style, morality, charisma, or doctrine in general.
- Sharper read: treat `rotating magnetic field` as one specific lever inside the wider system rather than the whole system.
- Corpus benefit: this distinction helps decide which quotes, cases, or texts actually belong under `rotating magnetic field`.
- Transfer benefit: it keeps modern analogies from importing too much historical baggage through `rotating magnetic field` by accident.
- Operational cue: define what `rotating magnetic field` is not before claiming to know what `rotating magnetic field` is.

### rotating magnetic field — Method

- Method question: how does `rotating magnetic field` actually work in texts, institutions, habits, or decisions rather than in rhetoric alone?
- Process lens: look for sequence, mechanism, routing, or recurrence inside `rotating magnetic field` rather than inspirational phrasing.
- Research use: `rotating magnetic field` matters when `rotating magnetic field` changes how attention, evidence, or action gets organized.
- Practical inference: the skill should borrow method from `rotating magnetic field`, not costume around `rotating magnetic field`.
- Good reading habit: ask which inputs, filters, and outputs are being structured by `rotating magnetic field`.
- Modern move: only transfer the mechanism in `rotating magnetic field` if you can name the real-world process it still corresponds to.
- Bad move: repeating the vocabulary of `rotating magnetic field` without reproducing the method inside `rotating magnetic field`.

### rotating magnetic field — Operational Use

- Operational role: `rotating magnetic field` belongs in the skill because `rotating magnetic field` changes how prompts are framed and how problems are cut.
- Decision value: `rotating magnetic field` sharpens what to inspect first, what to distrust, and what counts as progress.
- Prompt value: when `rotating magnetic field` is active, answers should become more selective about evidence and boundary conditions.
- Design value: `rotating magnetic field` can route users toward the right frame instead of merely giving them better wording.
- Testing value: `rotating magnetic field` suggests what kind of real example would confirm or falsify the current interpretation.
- Usage boundary: `rotating magnetic field` should guide reasoning, not become an ornamental tag glued to every response.
- Operator question: what does `rotating magnetic field` make me check before I act?

### rotating magnetic field — Transfer To Modern Problems

- Transfer principle: bring `rotating magnetic field` forward only when the structural analogy around `rotating magnetic field` is stronger than the historical costume.
- Modern fit: `rotating magnetic field` often transfers best into questions about decision quality, perception, incentives, coordination, or interpretation.
- Boundary line: do not smuggle era-specific authority relations from `rotating magnetic field` into contemporary settings that lack the same structure.
- Good transfer: extract the problem-solving intelligence in `rotating magnetic field`, not the surrounding mythology around `rotating magnetic field`.
- Bad transfer: using `rotating magnetic field` to justify positions `rotating magnetic field` was never built to adjudicate.
- Practical test: if the analogy for `rotating magnetic field` cannot survive plainer language, `rotating magnetic field` probably does not belong in the answer.
- Design implication: every modern use of `rotating magnetic field` should make the analogy explicit instead of pretending `rotating magnetic field` is self-evident.

### rotating magnetic field — Failure Mode

- Failure pattern: `rotating magnetic field` goes bad when `rotating magnetic field` becomes a shortcut to certainty rather than a pressure test.
- Common collapse: people borrow `rotating magnetic field` for authority, mood, or aesthetic and lose the underlying discipline in `rotating magnetic field`.
- Research signal: when `rotating magnetic field` is invoked too quickly, nuance disappears and contradiction gets cleaned out of `rotating magnetic field`.
- User risk: answers can start sounding impressive while `rotating magnetic field` becomes less falsifiable.
- Repository risk: `rotating magnetic field` can silently drift from operating concept into mascot.
- Correction move: pull `rotating magnetic field` back to evidence, mechanism, and concrete limits.
- Best warning label: `rotating magnetic field` should increase rigor, not lower the burden of proof.

### rotating magnetic field — Common Misreading

- Typical misreading: `rotating magnetic field` is often taken as a broad worldview when `rotating magnetic field` is actually one bounded instrument inside the larger system.
- Why this happens: selective quotation and later myth-making reward the simplest version of `rotating magnetic field`.
- Consequence: readers then import claims into `rotating magnetic field` that the corpus does not actually support for `rotating magnetic field`.
- Skill response: keep `rotating magnetic field` attached to the texts, cases, and institutional situations that make `rotating magnetic field` legible.
- Modern correction: show what `rotating magnetic field` can illuminate without pretending `rotating magnetic field` answers everything nearby.
- Language fix: name the overreach around `rotating magnetic field` directly instead of just gesturing at nuance.
- Reader test: if `rotating magnetic field` suddenly explains every problem, `rotating magnetic field` is being misread.

### rotating magnetic field — Boundary Condition

- Boundary condition: `rotating magnetic field` only travels well when the receiving problem shares enough structure with the original one behind `rotating magnetic field`.
- Context test: check scale, authority, evidence quality, and institutional shape before borrowing `rotating magnetic field`.
- Ethical test: some uses of `rotating magnetic field` cross from interpretation into endorsement, coercion, or myth-making.
- Skill discipline: boundary conditions stop the repo from turning `rotating magnetic field` into careless advice.
- Good limit: it is fine for `rotating magnetic field` to illuminate a narrow slice of the problem instead of the whole field.
- Bad limit: pretending `rotating magnetic field` has no boundary because `rotating magnetic field` feels generically wise.
- Operational question: under what conditions should `rotating magnetic field` be left unused?

### rotating magnetic field — Better Question

- Better question: instead of asking whether `rotating magnetic field` is simply true, ask what problem `rotating magnetic field` was trying to solve.
- Reader upgrade: this turns `rotating magnetic field` from a label into a live diagnostic instrument.
- Research upgrade: `rotating magnetic field` forces the file to connect claims to situations, stakes, and tradeoffs.
- Prompt upgrade: `rotating magnetic field` helps users formulate sharper questions about mechanism, motive, and transfer.
- Interpretive upgrade: `rotating magnetic field` keeps the answer from freezing into admiration or rejection too early.
- Skill upgrade: `rotating magnetic field` becomes useful when `rotating magnetic field` generates better questions faster than slogans.
- Best follow-up: what becomes newly visible once `rotating magnetic field` is framed as a problem-solving move?

### rotating magnetic field — Example Move

- Example move: use `rotating magnetic field` to reframe a messy issue into a smaller set of pressure points you can actually inspect.
- Practical scene: a good answer might use `rotating magnetic field` to separate motive from appearance, structure from story, or method from mythology.
- Why this works: `rotating magnetic field` is strongest when `rotating magnetic field` clarifies a live decision rather than decorating a summary.
- Demonstration value: an example move shows what `rotating magnetic field` lets the user do that they could not do before.
- Transfer discipline: keep the move around `rotating magnetic field` small and explicit so the analogy does not sprawl.
- Repository standard: if `rotating magnetic field` never produces a concrete move, `rotating magnetic field` is not yet operational.
- User question: what is one thing `rotating magnetic field` would make me inspect differently right now?

### rotating magnetic field — What To Preserve

- Preserve the pressure: `rotating magnetic field` should keep its original demand for discrimination, not just its vocabulary.
- Preserve the asymmetry: retain what `rotating magnetic field` notices earlier or more sharply than neighboring frames.
- Preserve the cost awareness: keep visible the tradeoff or risk that comes bundled with `rotating magnetic field`.
- Preserve the routing value: `rotating magnetic field` should still change what kind of evidence or example gets loaded first.
- Preserve the specificity: tie `rotating magnetic field` to actual texts, episodes, or institutional realities whenever possible.
- Preserve the limit: knowing where `rotating magnetic field` stops is part of preserving `rotating magnetic field` well.
- Preserve the usefulness: a transferred `rotating magnetic field` should still help someone make a cleaner judgment.

### rotating magnetic field — What To Drop

- Drop the costume: `rotating magnetic field` does not need borrowed historical atmosphere to remain useful.
- Drop the hero worship: admiration is not evidence that `rotating magnetic field` has been understood.
- Drop the universal claim: `rotating magnetic field` should not be stretched into a master key for unrelated problems.
- Drop the empty slogan: if `rotating magnetic field` survives only as a catchphrase, `rotating magnetic field` should be cut back to mechanism.
- Drop the false confidence: uncertainty about `rotating magnetic field` is often healthier than inflated clarity.
- Drop the prestige borrowing: do not let `rotating magnetic field` stand in for argument, sourcing, or proof.
- Drop the excess packaging: keep the operational core of `rotating magnetic field` and remove the surrounding noise.

### rotating magnetic field — Design Implication

- Design implication: `rotating magnetic field` should influence routing, prompt phrasing, and evidence selection inside the final skill.
- Repository implication: the file should store enough structure around `rotating magnetic field` that later edits can stay honest.
- UX implication: users should feel `rotating magnetic field` changing the angle of analysis, not just the tone.
- Evaluation implication: test `rotating magnetic field` on cases where naive admiration or rejection would both fail.
- Maintenance implication: future additions should land under `rotating magnetic field` only if they sharpen the boundary of `rotating magnetic field` rather than blur it.
- Safety implication: the skill must say what kinds of modern uses `rotating magnetic field` should refuse.
- Bottom line: `rotating magnetic field` belongs in the repo only if `rotating magnetic field` keeps improving judgment after the historical costume is removed.

### induction motor — Definition

- What this names: within `The engineering logic of Tesla's AC work, rotating fields, induction motors, and polyphase systems`, `induction motor` points to a concrete node rather than a prestige label.
- Object boundary: treat `induction motor` as a research handle with limits, not as a ready-made conclusion.
- Why it stays in the file: `induction motor` changes how the figure is periodized, sourced, or operationalized.
- How to read it: start from the historical referent of `induction motor` before pulling it into a modern analogy.
- Skill value: `induction motor` gives the repo a sharper diagnostic vocabulary for judgment and transfer.
- Reader warning: when `induction motor` is reduced to a slogan, the file loses the distinction it was built to protect.
- Practical use: define `induction motor` clearly enough that a later answer can say what `induction motor` includes and excludes.

### induction motor — Historical Context

- Historical location: ask when `induction motor` first becomes visible in the surviving record and through which institution, text, or episode.
- Surrounding pressure: `induction motor` usually sits inside a wider dispute about authority, method, legitimacy, or transmission.
- Context clue: the point is not only what `induction motor` says, but why `induction motor` mattered in its own setting.
- Corpus implication: historical placement helps distinguish stable doctrine around `induction motor` from later packaging or selective reuse.
- Reading move: map `induction motor` against the timeline before treating `induction motor` as timeless method.
- Transfer discipline: preserve the contextual problem that produced `induction motor` before extracting any lesson from `induction motor`.
- Failure risk: without context, `induction motor` can look cleaner, more universal, and more innocent than it actually was.

### induction motor — Internal Tension

- Main tension: `induction motor` usually bundles a capability gain with a distortion cost instead of offering one clean virtue.
- Diagnostic split: ask what `induction motor` makes possible and what `induction motor` quietly suppresses at the same time.
- Interpretive value: the tension in `induction motor` matters because this repo is meant to preserve pressure, not just preference.
- Operational lesson: strong skills keep `induction motor` double-sided rather than converting `induction motor` into moral reassurance.
- Research warning: if every sentence about `induction motor` sounds admiring or dismissive, `induction motor` has already been flattened.
- Transfer rule: modern answers should name both the leverage and the blind spot living inside `induction motor`.
- Better question: what does `induction motor` solve, and what new silence does `induction motor` create?

### induction motor — Key Distinction

- Core distinction: separate `induction motor` from adjacent ideas that look similar but solve a different problem.
- Why this matters: a person skill only becomes precise when near-miss concepts stop leaking into `induction motor`.
- Typical confusion: readers often mistake `induction motor` for style, morality, charisma, or doctrine in general.
- Sharper read: treat `induction motor` as one specific lever inside the wider system rather than the whole system.
- Corpus benefit: this distinction helps decide which quotes, cases, or texts actually belong under `induction motor`.
- Transfer benefit: it keeps modern analogies from importing too much historical baggage through `induction motor` by accident.
- Operational cue: define what `induction motor` is not before claiming to know what `induction motor` is.

### induction motor — Method

- Method question: how does `induction motor` actually work in texts, institutions, habits, or decisions rather than in rhetoric alone?
- Process lens: look for sequence, mechanism, routing, or recurrence inside `induction motor` rather than inspirational phrasing.
- Research use: `induction motor` matters when `induction motor` changes how attention, evidence, or action gets organized.
- Practical inference: the skill should borrow method from `induction motor`, not costume around `induction motor`.
- Good reading habit: ask which inputs, filters, and outputs are being structured by `induction motor`.
- Modern move: only transfer the mechanism in `induction motor` if you can name the real-world process it still corresponds to.
- Bad move: repeating the vocabulary of `induction motor` without reproducing the method inside `induction motor`.

### induction motor — Operational Use

- Operational role: `induction motor` belongs in the skill because `induction motor` changes how prompts are framed and how problems are cut.
- Decision value: `induction motor` sharpens what to inspect first, what to distrust, and what counts as progress.
- Prompt value: when `induction motor` is active, answers should become more selective about evidence and boundary conditions.
- Design value: `induction motor` can route users toward the right frame instead of merely giving them better wording.
- Testing value: `induction motor` suggests what kind of real example would confirm or falsify the current interpretation.
- Usage boundary: `induction motor` should guide reasoning, not become an ornamental tag glued to every response.
- Operator question: what does `induction motor` make me check before I act?

### induction motor — Transfer To Modern Problems

- Transfer principle: bring `induction motor` forward only when the structural analogy around `induction motor` is stronger than the historical costume.
- Modern fit: `induction motor` often transfers best into questions about decision quality, perception, incentives, coordination, or interpretation.
- Boundary line: do not smuggle era-specific authority relations from `induction motor` into contemporary settings that lack the same structure.
- Good transfer: extract the problem-solving intelligence in `induction motor`, not the surrounding mythology around `induction motor`.
- Bad transfer: using `induction motor` to justify positions `induction motor` was never built to adjudicate.
- Practical test: if the analogy for `induction motor` cannot survive plainer language, `induction motor` probably does not belong in the answer.
- Design implication: every modern use of `induction motor` should make the analogy explicit instead of pretending `induction motor` is self-evident.

### induction motor — Failure Mode

- Failure pattern: `induction motor` goes bad when `induction motor` becomes a shortcut to certainty rather than a pressure test.
- Common collapse: people borrow `induction motor` for authority, mood, or aesthetic and lose the underlying discipline in `induction motor`.
- Research signal: when `induction motor` is invoked too quickly, nuance disappears and contradiction gets cleaned out of `induction motor`.
- User risk: answers can start sounding impressive while `induction motor` becomes less falsifiable.
- Repository risk: `induction motor` can silently drift from operating concept into mascot.
- Correction move: pull `induction motor` back to evidence, mechanism, and concrete limits.
- Best warning label: `induction motor` should increase rigor, not lower the burden of proof.

### induction motor — Common Misreading

- Typical misreading: `induction motor` is often taken as a broad worldview when `induction motor` is actually one bounded instrument inside the larger system.
- Why this happens: selective quotation and later myth-making reward the simplest version of `induction motor`.
- Consequence: readers then import claims into `induction motor` that the corpus does not actually support for `induction motor`.
- Skill response: keep `induction motor` attached to the texts, cases, and institutional situations that make `induction motor` legible.
- Modern correction: show what `induction motor` can illuminate without pretending `induction motor` answers everything nearby.
- Language fix: name the overreach around `induction motor` directly instead of just gesturing at nuance.
- Reader test: if `induction motor` suddenly explains every problem, `induction motor` is being misread.

### induction motor — Boundary Condition

- Boundary condition: `induction motor` only travels well when the receiving problem shares enough structure with the original one behind `induction motor`.
- Context test: check scale, authority, evidence quality, and institutional shape before borrowing `induction motor`.
- Ethical test: some uses of `induction motor` cross from interpretation into endorsement, coercion, or myth-making.
- Skill discipline: boundary conditions stop the repo from turning `induction motor` into careless advice.
- Good limit: it is fine for `induction motor` to illuminate a narrow slice of the problem instead of the whole field.
- Bad limit: pretending `induction motor` has no boundary because `induction motor` feels generically wise.
- Operational question: under what conditions should `induction motor` be left unused?

### induction motor — Better Question

- Better question: instead of asking whether `induction motor` is simply true, ask what problem `induction motor` was trying to solve.
- Reader upgrade: this turns `induction motor` from a label into a live diagnostic instrument.
- Research upgrade: `induction motor` forces the file to connect claims to situations, stakes, and tradeoffs.
- Prompt upgrade: `induction motor` helps users formulate sharper questions about mechanism, motive, and transfer.
- Interpretive upgrade: `induction motor` keeps the answer from freezing into admiration or rejection too early.
- Skill upgrade: `induction motor` becomes useful when `induction motor` generates better questions faster than slogans.
- Best follow-up: what becomes newly visible once `induction motor` is framed as a problem-solving move?

### induction motor — Example Move

- Example move: use `induction motor` to reframe a messy issue into a smaller set of pressure points you can actually inspect.
- Practical scene: a good answer might use `induction motor` to separate motive from appearance, structure from story, or method from mythology.
- Why this works: `induction motor` is strongest when `induction motor` clarifies a live decision rather than decorating a summary.
- Demonstration value: an example move shows what `induction motor` lets the user do that they could not do before.
- Transfer discipline: keep the move around `induction motor` small and explicit so the analogy does not sprawl.
- Repository standard: if `induction motor` never produces a concrete move, `induction motor` is not yet operational.
- User question: what is one thing `induction motor` would make me inspect differently right now?

### induction motor — What To Preserve

- Preserve the pressure: `induction motor` should keep its original demand for discrimination, not just its vocabulary.
- Preserve the asymmetry: retain what `induction motor` notices earlier or more sharply than neighboring frames.
- Preserve the cost awareness: keep visible the tradeoff or risk that comes bundled with `induction motor`.
- Preserve the routing value: `induction motor` should still change what kind of evidence or example gets loaded first.
- Preserve the specificity: tie `induction motor` to actual texts, episodes, or institutional realities whenever possible.
- Preserve the limit: knowing where `induction motor` stops is part of preserving `induction motor` well.
- Preserve the usefulness: a transferred `induction motor` should still help someone make a cleaner judgment.

### induction motor — What To Drop

- Drop the costume: `induction motor` does not need borrowed historical atmosphere to remain useful.
- Drop the hero worship: admiration is not evidence that `induction motor` has been understood.
- Drop the universal claim: `induction motor` should not be stretched into a master key for unrelated problems.
- Drop the empty slogan: if `induction motor` survives only as a catchphrase, `induction motor` should be cut back to mechanism.
- Drop the false confidence: uncertainty about `induction motor` is often healthier than inflated clarity.
- Drop the prestige borrowing: do not let `induction motor` stand in for argument, sourcing, or proof.
- Drop the excess packaging: keep the operational core of `induction motor` and remove the surrounding noise.

### induction motor — Design Implication

- Design implication: `induction motor` should influence routing, prompt phrasing, and evidence selection inside the final skill.
- Repository implication: the file should store enough structure around `induction motor` that later edits can stay honest.
- UX implication: users should feel `induction motor` changing the angle of analysis, not just the tone.
- Evaluation implication: test `induction motor` on cases where naive admiration or rejection would both fail.
- Maintenance implication: future additions should land under `induction motor` only if they sharpen the boundary of `induction motor` rather than blur it.
- Safety implication: the skill must say what kinds of modern uses `induction motor` should refuse.
- Bottom line: `induction motor` belongs in the repo only if `induction motor` keeps improving judgment after the historical costume is removed.

### polyphase systems — Definition

- What this names: within `The engineering logic of Tesla's AC work, rotating fields, induction motors, and polyphase systems`, `polyphase systems` points to a concrete node rather than a prestige label.
- Object boundary: treat `polyphase systems` as a research handle with limits, not as a ready-made conclusion.
- Why it stays in the file: `polyphase systems` changes how the figure is periodized, sourced, or operationalized.
- How to read it: start from the historical referent of `polyphase systems` before pulling it into a modern analogy.
- Skill value: `polyphase systems` gives the repo a sharper diagnostic vocabulary for judgment and transfer.
- Reader warning: when `polyphase systems` is reduced to a slogan, the file loses the distinction it was built to protect.
- Practical use: define `polyphase systems` clearly enough that a later answer can say what `polyphase systems` includes and excludes.

### polyphase systems — Historical Context

- Historical location: ask when `polyphase systems` first becomes visible in the surviving record and through which institution, text, or episode.
- Surrounding pressure: `polyphase systems` usually sits inside a wider dispute about authority, method, legitimacy, or transmission.
- Context clue: the point is not only what `polyphase systems` says, but why `polyphase systems` mattered in its own setting.
- Corpus implication: historical placement helps distinguish stable doctrine around `polyphase systems` from later packaging or selective reuse.
- Reading move: map `polyphase systems` against the timeline before treating `polyphase systems` as timeless method.
- Transfer discipline: preserve the contextual problem that produced `polyphase systems` before extracting any lesson from `polyphase systems`.
- Failure risk: without context, `polyphase systems` can look cleaner, more universal, and more innocent than it actually was.

### polyphase systems — Internal Tension

- Main tension: `polyphase systems` usually bundles a capability gain with a distortion cost instead of offering one clean virtue.
- Diagnostic split: ask what `polyphase systems` makes possible and what `polyphase systems` quietly suppresses at the same time.
- Interpretive value: the tension in `polyphase systems` matters because this repo is meant to preserve pressure, not just preference.
- Operational lesson: strong skills keep `polyphase systems` double-sided rather than converting `polyphase systems` into moral reassurance.
- Research warning: if every sentence about `polyphase systems` sounds admiring or dismissive, `polyphase systems` has already been flattened.
- Transfer rule: modern answers should name both the leverage and the blind spot living inside `polyphase systems`.
- Better question: what does `polyphase systems` solve, and what new silence does `polyphase systems` create?

### polyphase systems — Key Distinction

- Core distinction: separate `polyphase systems` from adjacent ideas that look similar but solve a different problem.
- Why this matters: a person skill only becomes precise when near-miss concepts stop leaking into `polyphase systems`.
- Typical confusion: readers often mistake `polyphase systems` for style, morality, charisma, or doctrine in general.
- Sharper read: treat `polyphase systems` as one specific lever inside the wider system rather than the whole system.
- Corpus benefit: this distinction helps decide which quotes, cases, or texts actually belong under `polyphase systems`.
- Transfer benefit: it keeps modern analogies from importing too much historical baggage through `polyphase systems` by accident.
- Operational cue: define what `polyphase systems` is not before claiming to know what `polyphase systems` is.

### polyphase systems — Method

- Method question: how does `polyphase systems` actually work in texts, institutions, habits, or decisions rather than in rhetoric alone?
- Process lens: look for sequence, mechanism, routing, or recurrence inside `polyphase systems` rather than inspirational phrasing.
- Research use: `polyphase systems` matters when `polyphase systems` changes how attention, evidence, or action gets organized.
- Practical inference: the skill should borrow method from `polyphase systems`, not costume around `polyphase systems`.
- Good reading habit: ask which inputs, filters, and outputs are being structured by `polyphase systems`.
- Modern move: only transfer the mechanism in `polyphase systems` if you can name the real-world process it still corresponds to.
- Bad move: repeating the vocabulary of `polyphase systems` without reproducing the method inside `polyphase systems`.

### polyphase systems — Operational Use

- Operational role: `polyphase systems` belongs in the skill because `polyphase systems` changes how prompts are framed and how problems are cut.
- Decision value: `polyphase systems` sharpens what to inspect first, what to distrust, and what counts as progress.
- Prompt value: when `polyphase systems` is active, answers should become more selective about evidence and boundary conditions.
- Design value: `polyphase systems` can route users toward the right frame instead of merely giving them better wording.
- Testing value: `polyphase systems` suggests what kind of real example would confirm or falsify the current interpretation.
- Usage boundary: `polyphase systems` should guide reasoning, not become an ornamental tag glued to every response.
- Operator question: what does `polyphase systems` make me check before I act?

### polyphase systems — Transfer To Modern Problems

- Transfer principle: bring `polyphase systems` forward only when the structural analogy around `polyphase systems` is stronger than the historical costume.
- Modern fit: `polyphase systems` often transfers best into questions about decision quality, perception, incentives, coordination, or interpretation.
- Boundary line: do not smuggle era-specific authority relations from `polyphase systems` into contemporary settings that lack the same structure.
- Good transfer: extract the problem-solving intelligence in `polyphase systems`, not the surrounding mythology around `polyphase systems`.
- Bad transfer: using `polyphase systems` to justify positions `polyphase systems` was never built to adjudicate.
- Practical test: if the analogy for `polyphase systems` cannot survive plainer language, `polyphase systems` probably does not belong in the answer.
- Design implication: every modern use of `polyphase systems` should make the analogy explicit instead of pretending `polyphase systems` is self-evident.

### polyphase systems — Failure Mode

- Failure pattern: `polyphase systems` goes bad when `polyphase systems` becomes a shortcut to certainty rather than a pressure test.
- Common collapse: people borrow `polyphase systems` for authority, mood, or aesthetic and lose the underlying discipline in `polyphase systems`.
- Research signal: when `polyphase systems` is invoked too quickly, nuance disappears and contradiction gets cleaned out of `polyphase systems`.
- User risk: answers can start sounding impressive while `polyphase systems` becomes less falsifiable.
- Repository risk: `polyphase systems` can silently drift from operating concept into mascot.
- Correction move: pull `polyphase systems` back to evidence, mechanism, and concrete limits.
- Best warning label: `polyphase systems` should increase rigor, not lower the burden of proof.

### polyphase systems — Common Misreading

- Typical misreading: `polyphase systems` is often taken as a broad worldview when `polyphase systems` is actually one bounded instrument inside the larger system.
- Why this happens: selective quotation and later myth-making reward the simplest version of `polyphase systems`.
- Consequence: readers then import claims into `polyphase systems` that the corpus does not actually support for `polyphase systems`.
- Skill response: keep `polyphase systems` attached to the texts, cases, and institutional situations that make `polyphase systems` legible.
- Modern correction: show what `polyphase systems` can illuminate without pretending `polyphase systems` answers everything nearby.
- Language fix: name the overreach around `polyphase systems` directly instead of just gesturing at nuance.
- Reader test: if `polyphase systems` suddenly explains every problem, `polyphase systems` is being misread.

### polyphase systems — Boundary Condition

- Boundary condition: `polyphase systems` only travels well when the receiving problem shares enough structure with the original one behind `polyphase systems`.
- Context test: check scale, authority, evidence quality, and institutional shape before borrowing `polyphase systems`.
- Ethical test: some uses of `polyphase systems` cross from interpretation into endorsement, coercion, or myth-making.
- Skill discipline: boundary conditions stop the repo from turning `polyphase systems` into careless advice.
- Good limit: it is fine for `polyphase systems` to illuminate a narrow slice of the problem instead of the whole field.
- Bad limit: pretending `polyphase systems` has no boundary because `polyphase systems` feels generically wise.
- Operational question: under what conditions should `polyphase systems` be left unused?

### polyphase systems — Better Question

- Better question: instead of asking whether `polyphase systems` is simply true, ask what problem `polyphase systems` was trying to solve.
- Reader upgrade: this turns `polyphase systems` from a label into a live diagnostic instrument.
- Research upgrade: `polyphase systems` forces the file to connect claims to situations, stakes, and tradeoffs.
- Prompt upgrade: `polyphase systems` helps users formulate sharper questions about mechanism, motive, and transfer.
- Interpretive upgrade: `polyphase systems` keeps the answer from freezing into admiration or rejection too early.
- Skill upgrade: `polyphase systems` becomes useful when `polyphase systems` generates better questions faster than slogans.
- Best follow-up: what becomes newly visible once `polyphase systems` is framed as a problem-solving move?

### polyphase systems — Example Move

- Example move: use `polyphase systems` to reframe a messy issue into a smaller set of pressure points you can actually inspect.
- Practical scene: a good answer might use `polyphase systems` to separate motive from appearance, structure from story, or method from mythology.
- Why this works: `polyphase systems` is strongest when `polyphase systems` clarifies a live decision rather than decorating a summary.
- Demonstration value: an example move shows what `polyphase systems` lets the user do that they could not do before.
- Transfer discipline: keep the move around `polyphase systems` small and explicit so the analogy does not sprawl.
- Repository standard: if `polyphase systems` never produces a concrete move, `polyphase systems` is not yet operational.
- User question: what is one thing `polyphase systems` would make me inspect differently right now?

### polyphase systems — What To Preserve

- Preserve the pressure: `polyphase systems` should keep its original demand for discrimination, not just its vocabulary.
- Preserve the asymmetry: retain what `polyphase systems` notices earlier or more sharply than neighboring frames.
- Preserve the cost awareness: keep visible the tradeoff or risk that comes bundled with `polyphase systems`.
- Preserve the routing value: `polyphase systems` should still change what kind of evidence or example gets loaded first.
- Preserve the specificity: tie `polyphase systems` to actual texts, episodes, or institutional realities whenever possible.
- Preserve the limit: knowing where `polyphase systems` stops is part of preserving `polyphase systems` well.
- Preserve the usefulness: a transferred `polyphase systems` should still help someone make a cleaner judgment.

### polyphase systems — What To Drop

- Drop the costume: `polyphase systems` does not need borrowed historical atmosphere to remain useful.
- Drop the hero worship: admiration is not evidence that `polyphase systems` has been understood.
- Drop the universal claim: `polyphase systems` should not be stretched into a master key for unrelated problems.
- Drop the empty slogan: if `polyphase systems` survives only as a catchphrase, `polyphase systems` should be cut back to mechanism.
- Drop the false confidence: uncertainty about `polyphase systems` is often healthier than inflated clarity.
- Drop the prestige borrowing: do not let `polyphase systems` stand in for argument, sourcing, or proof.
- Drop the excess packaging: keep the operational core of `polyphase systems` and remove the surrounding noise.

### polyphase systems — Design Implication

- Design implication: `polyphase systems` should influence routing, prompt phrasing, and evidence selection inside the final skill.
- Repository implication: the file should store enough structure around `polyphase systems` that later edits can stay honest.
- UX implication: users should feel `polyphase systems` changing the angle of analysis, not just the tone.
- Evaluation implication: test `polyphase systems` on cases where naive admiration or rejection would both fail.
- Maintenance implication: future additions should land under `polyphase systems` only if they sharpen the boundary of `polyphase systems` rather than blur it.
- Safety implication: the skill must say what kinds of modern uses `polyphase systems` should refuse.
- Bottom line: `polyphase systems` belongs in the repo only if `polyphase systems` keeps improving judgment after the historical costume is removed.

### system-level advantage — Definition

- What this names: within `The engineering logic of Tesla's AC work, rotating fields, induction motors, and polyphase systems`, `system-level advantage` points to a concrete node rather than a prestige label.
- Object boundary: treat `system-level advantage` as a research handle with limits, not as a ready-made conclusion.
- Why it stays in the file: `system-level advantage` changes how the figure is periodized, sourced, or operationalized.
- How to read it: start from the historical referent of `system-level advantage` before pulling it into a modern analogy.
- Skill value: `system-level advantage` gives the repo a sharper diagnostic vocabulary for judgment and transfer.
- Reader warning: when `system-level advantage` is reduced to a slogan, the file loses the distinction it was built to protect.
- Practical use: define `system-level advantage` clearly enough that a later answer can say what `system-level advantage` includes and excludes.

### system-level advantage — Historical Context

- Historical location: ask when `system-level advantage` first becomes visible in the surviving record and through which institution, text, or episode.
- Surrounding pressure: `system-level advantage` usually sits inside a wider dispute about authority, method, legitimacy, or transmission.
- Context clue: the point is not only what `system-level advantage` says, but why `system-level advantage` mattered in its own setting.
- Corpus implication: historical placement helps distinguish stable doctrine around `system-level advantage` from later packaging or selective reuse.
- Reading move: map `system-level advantage` against the timeline before treating `system-level advantage` as timeless method.
- Transfer discipline: preserve the contextual problem that produced `system-level advantage` before extracting any lesson from `system-level advantage`.
- Failure risk: without context, `system-level advantage` can look cleaner, more universal, and more innocent than it actually was.

### system-level advantage — Internal Tension

- Main tension: `system-level advantage` usually bundles a capability gain with a distortion cost instead of offering one clean virtue.
- Diagnostic split: ask what `system-level advantage` makes possible and what `system-level advantage` quietly suppresses at the same time.
- Interpretive value: the tension in `system-level advantage` matters because this repo is meant to preserve pressure, not just preference.
- Operational lesson: strong skills keep `system-level advantage` double-sided rather than converting `system-level advantage` into moral reassurance.
- Research warning: if every sentence about `system-level advantage` sounds admiring or dismissive, `system-level advantage` has already been flattened.
- Transfer rule: modern answers should name both the leverage and the blind spot living inside `system-level advantage`.
- Better question: what does `system-level advantage` solve, and what new silence does `system-level advantage` create?

### system-level advantage — Key Distinction

- Core distinction: separate `system-level advantage` from adjacent ideas that look similar but solve a different problem.
- Why this matters: a person skill only becomes precise when near-miss concepts stop leaking into `system-level advantage`.
- Typical confusion: readers often mistake `system-level advantage` for style, morality, charisma, or doctrine in general.
- Sharper read: treat `system-level advantage` as one specific lever inside the wider system rather than the whole system.
- Corpus benefit: this distinction helps decide which quotes, cases, or texts actually belong under `system-level advantage`.
- Transfer benefit: it keeps modern analogies from importing too much historical baggage through `system-level advantage` by accident.
- Operational cue: define what `system-level advantage` is not before claiming to know what `system-level advantage` is.

### system-level advantage — Method

- Method question: how does `system-level advantage` actually work in texts, institutions, habits, or decisions rather than in rhetoric alone?
- Process lens: look for sequence, mechanism, routing, or recurrence inside `system-level advantage` rather than inspirational phrasing.
- Research use: `system-level advantage` matters when `system-level advantage` changes how attention, evidence, or action gets organized.
- Practical inference: the skill should borrow method from `system-level advantage`, not costume around `system-level advantage`.
- Good reading habit: ask which inputs, filters, and outputs are being structured by `system-level advantage`.
- Modern move: only transfer the mechanism in `system-level advantage` if you can name the real-world process it still corresponds to.
- Bad move: repeating the vocabulary of `system-level advantage` without reproducing the method inside `system-level advantage`.

### system-level advantage — Operational Use

- Operational role: `system-level advantage` belongs in the skill because `system-level advantage` changes how prompts are framed and how problems are cut.
- Decision value: `system-level advantage` sharpens what to inspect first, what to distrust, and what counts as progress.
- Prompt value: when `system-level advantage` is active, answers should become more selective about evidence and boundary conditions.
- Design value: `system-level advantage` can route users toward the right frame instead of merely giving them better wording.
- Testing value: `system-level advantage` suggests what kind of real example would confirm or falsify the current interpretation.
- Usage boundary: `system-level advantage` should guide reasoning, not become an ornamental tag glued to every response.
- Operator question: what does `system-level advantage` make me check before I act?

### system-level advantage — Transfer To Modern Problems

- Transfer principle: bring `system-level advantage` forward only when the structural analogy around `system-level advantage` is stronger than the historical costume.
- Modern fit: `system-level advantage` often transfers best into questions about decision quality, perception, incentives, coordination, or interpretation.
- Boundary line: do not smuggle era-specific authority relations from `system-level advantage` into contemporary settings that lack the same structure.
- Good transfer: extract the problem-solving intelligence in `system-level advantage`, not the surrounding mythology around `system-level advantage`.
- Bad transfer: using `system-level advantage` to justify positions `system-level advantage` was never built to adjudicate.
- Practical test: if the analogy for `system-level advantage` cannot survive plainer language, `system-level advantage` probably does not belong in the answer.
- Design implication: every modern use of `system-level advantage` should make the analogy explicit instead of pretending `system-level advantage` is self-evident.

### system-level advantage — Failure Mode

- Failure pattern: `system-level advantage` goes bad when `system-level advantage` becomes a shortcut to certainty rather than a pressure test.
- Common collapse: people borrow `system-level advantage` for authority, mood, or aesthetic and lose the underlying discipline in `system-level advantage`.
- Research signal: when `system-level advantage` is invoked too quickly, nuance disappears and contradiction gets cleaned out of `system-level advantage`.
- User risk: answers can start sounding impressive while `system-level advantage` becomes less falsifiable.
- Repository risk: `system-level advantage` can silently drift from operating concept into mascot.
- Correction move: pull `system-level advantage` back to evidence, mechanism, and concrete limits.
- Best warning label: `system-level advantage` should increase rigor, not lower the burden of proof.

### system-level advantage — Common Misreading

- Typical misreading: `system-level advantage` is often taken as a broad worldview when `system-level advantage` is actually one bounded instrument inside the larger system.
- Why this happens: selective quotation and later myth-making reward the simplest version of `system-level advantage`.
- Consequence: readers then import claims into `system-level advantage` that the corpus does not actually support for `system-level advantage`.
- Skill response: keep `system-level advantage` attached to the texts, cases, and institutional situations that make `system-level advantage` legible.
- Modern correction: show what `system-level advantage` can illuminate without pretending `system-level advantage` answers everything nearby.
- Language fix: name the overreach around `system-level advantage` directly instead of just gesturing at nuance.
- Reader test: if `system-level advantage` suddenly explains every problem, `system-level advantage` is being misread.

### system-level advantage — Boundary Condition

- Boundary condition: `system-level advantage` only travels well when the receiving problem shares enough structure with the original one behind `system-level advantage`.
- Context test: check scale, authority, evidence quality, and institutional shape before borrowing `system-level advantage`.
- Ethical test: some uses of `system-level advantage` cross from interpretation into endorsement, coercion, or myth-making.
- Skill discipline: boundary conditions stop the repo from turning `system-level advantage` into careless advice.
- Good limit: it is fine for `system-level advantage` to illuminate a narrow slice of the problem instead of the whole field.
- Bad limit: pretending `system-level advantage` has no boundary because `system-level advantage` feels generically wise.
- Operational question: under what conditions should `system-level advantage` be left unused?

### system-level advantage — Better Question

- Better question: instead of asking whether `system-level advantage` is simply true, ask what problem `system-level advantage` was trying to solve.
- Reader upgrade: this turns `system-level advantage` from a label into a live diagnostic instrument.
- Research upgrade: `system-level advantage` forces the file to connect claims to situations, stakes, and tradeoffs.
- Prompt upgrade: `system-level advantage` helps users formulate sharper questions about mechanism, motive, and transfer.
- Interpretive upgrade: `system-level advantage` keeps the answer from freezing into admiration or rejection too early.
- Skill upgrade: `system-level advantage` becomes useful when `system-level advantage` generates better questions faster than slogans.
- Best follow-up: what becomes newly visible once `system-level advantage` is framed as a problem-solving move?

### system-level advantage — Example Move

- Example move: use `system-level advantage` to reframe a messy issue into a smaller set of pressure points you can actually inspect.
- Practical scene: a good answer might use `system-level advantage` to separate motive from appearance, structure from story, or method from mythology.
- Why this works: `system-level advantage` is strongest when `system-level advantage` clarifies a live decision rather than decorating a summary.
- Demonstration value: an example move shows what `system-level advantage` lets the user do that they could not do before.
- Transfer discipline: keep the move around `system-level advantage` small and explicit so the analogy does not sprawl.
- Repository standard: if `system-level advantage` never produces a concrete move, `system-level advantage` is not yet operational.
- User question: what is one thing `system-level advantage` would make me inspect differently right now?

### system-level advantage — What To Preserve

- Preserve the pressure: `system-level advantage` should keep its original demand for discrimination, not just its vocabulary.
- Preserve the asymmetry: retain what `system-level advantage` notices earlier or more sharply than neighboring frames.
- Preserve the cost awareness: keep visible the tradeoff or risk that comes bundled with `system-level advantage`.
- Preserve the routing value: `system-level advantage` should still change what kind of evidence or example gets loaded first.
- Preserve the specificity: tie `system-level advantage` to actual texts, episodes, or institutional realities whenever possible.
- Preserve the limit: knowing where `system-level advantage` stops is part of preserving `system-level advantage` well.
- Preserve the usefulness: a transferred `system-level advantage` should still help someone make a cleaner judgment.

### system-level advantage — What To Drop

- Drop the costume: `system-level advantage` does not need borrowed historical atmosphere to remain useful.
- Drop the hero worship: admiration is not evidence that `system-level advantage` has been understood.
- Drop the universal claim: `system-level advantage` should not be stretched into a master key for unrelated problems.
- Drop the empty slogan: if `system-level advantage` survives only as a catchphrase, `system-level advantage` should be cut back to mechanism.
- Drop the false confidence: uncertainty about `system-level advantage` is often healthier than inflated clarity.
- Drop the prestige borrowing: do not let `system-level advantage` stand in for argument, sourcing, or proof.
- Drop the excess packaging: keep the operational core of `system-level advantage` and remove the surrounding noise.

### system-level advantage — Design Implication

- Design implication: `system-level advantage` should influence routing, prompt phrasing, and evidence selection inside the final skill.
- Repository implication: the file should store enough structure around `system-level advantage` that later edits can stay honest.
- UX implication: users should feel `system-level advantage` changing the angle of analysis, not just the tone.
- Evaluation implication: test `system-level advantage` on cases where naive admiration or rejection would both fail.
- Maintenance implication: future additions should land under `system-level advantage` only if they sharpen the boundary of `system-level advantage` rather than blur it.
- Safety implication: the skill must say what kinds of modern uses `system-level advantage` should refuse.
- Bottom line: `system-level advantage` belongs in the repo only if `system-level advantage` keeps improving judgment after the historical costume is removed.

## 原始素材文件

这份 research 直接回链到本仓库的原始素材层：

- books：[my-inventions.md](../sources/books/my-inventions.md)
- transcripts：[my-inventions-mental-simulation.md](../sources/transcripts/my-inventions-mental-simulation.md)、[patent-and-demo-voice-source-card.md](../sources/transcripts/patent-and-demo-voice-source-card.md)、[problem-of-increasing-human-energy.md](../sources/transcripts/problem-of-increasing-human-energy.md)
- articles：[britannica-tesla.md](../sources/articles/britannica-tesla.md)、[tesla-patent-index.md](../sources/articles/tesla-patent-index.md)、[tesla-universe-biography.md](../sources/articles/tesla-universe-biography.md)、[teslauniverse-my-inventions-page.md](../sources/articles/teslauniverse-my-inventions-page.md)
